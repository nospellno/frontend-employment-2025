import React from 'react';

const Group2Layout = ({ children }: { children: React.ReactNode }) => {
  return (
    <section>
      <h1>Group 2 레이아웃입니다.</h1>
      {children}
    </section>
  );
};

export default Group2Layout;
