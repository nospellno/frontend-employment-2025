import React from 'react';

const Group3Layout = ({ children }: { children: React.ReactNode }) => {
  return (
    <section>
      <h1>Group 3 레이아웃입니다.</h1>
      {children}
    </section>
  );
};

export default Group3Layout;
